"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
import { navigation } from "@/data/content";
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-stone-800/10 bg-[#f8f5f0]/95 backdrop-blur"><div className="wrap flex h-18 items-center justify-between px-5"><Link href="/" className="display text-2xl tracking-[-.08em]" aria-label="ÉLAN Clinic, головна">ÉLAN<span className="text-stone-500">.</span></Link><nav className="hidden items-center gap-6 lg:flex" aria-label="Основна навігація">{navigation.map(item=><a className="text-xs font-bold uppercase tracking-[.12em] hover:text-stone-500" href={item.href} key={item.href}>{item.label}</a>)}</nav><a className="button hidden md:inline-flex" href="#appointment">Записатися</a><button onClick={()=>setOpen(!open)} className="lg:hidden" aria-label={open?"Закрити меню":"Відкрити меню"}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-stone-800/10 bg-[#f8f5f0] px-5 py-5 lg:hidden" aria-label="Мобільна навігація">{navigation.map(item=><a onClick={()=>setOpen(false)} className="block border-b border-stone-800/10 py-4 text-sm font-bold" href={item.href} key={item.href}>{item.label}</a>)}<a onClick={()=>setOpen(false)} className="button mt-5 w-full" href="#appointment">Записатися на консультацію</a></nav>}</header>}
