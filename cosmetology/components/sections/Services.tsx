import Image from "next/image";
import { ArrowUpRight } from "lucide-react";
import { services } from "@/data/content";
import { Reveal } from "@/components/ui/Reveal";
export function Services(){return <section id="procedures" className="section"><div className="wrap"><Reveal><div className="mb-12 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow">Напрями роботи</p><h2 className="display mt-4 max-w-2xl text-4xl sm:text-5xl">Процедури, що підтримують якість шкіри</h2></div><a href="#appointment" className="text-sm font-bold underline underline-offset-5">Підібрати процедуру</a></div></Reveal><div className="grid gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{services.map((service,index)=><Reveal key={service.title}><article className={index===0?"sm:col-span-2 lg:col-span-1":""}><div className="relative aspect-[4/3] overflow-hidden"><Image fill sizes="(max-width: 640px) 100vw, (max-width:1024px) 50vw, 33vw" className="object-cover transition duration-500 hover:scale-105" src={service.image} alt={service.title}/></div><div className="border-b rule py-4"><div className="flex items-start justify-between gap-4"><h3 className="display text-2xl">{service.title}</h3><ArrowUpRight size={18}/></div><p className="mt-3 max-w-sm text-sm leading-6 text-stone-600">{service.description}</p><div className="mt-5 flex justify-between text-xs font-bold uppercase tracking-wider"><span>{service.duration}</span><span>{service.from}</span></div></div></article></Reveal>)}</div></div></section>}
