# Nord Form

Сайт виробника меблів на замовлення. Команди: `npm install`, `npm run dev`, `npm run lint`, `npm run typecheck`, `npm run build`. Проєкти й категорії — `data/content.ts`; контакти — футер; інтеграцію форми додайте в `components/sections/Content.tsx`.
