"use client";import Link from "next/link";import {Menu,X} from "lucide-react";import {useState} from "react";import {navigation} from "@/data/content";
export function Header(){const[open,setOpen]=useState(false);return <header className="absolute inset-x-0 top-0 z-20 text-white"><div className="wrap flex h-20 items-center justify-between px-5"><Link href="/" className="display text-2xl">TerraNova<span className="text-[#c8b36c]">.</span></Link><nav className="hidden gap-6 lg:flex" aria-label="Основна навігація">{navigation.map(x=><a className="text-xs font-bold uppercase tracking-wider hover:text-[#d5caab]" key={x.href} href={x.href}>{x.label}</a>)}</nav><a className="button hidden md:inline-flex" href="#proposal">Стати партнером</a><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label={open?"Закрити меню":"Відкрити меню"}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="bg-[#1f392e] px-5 pb-6 lg:hidden">{navigation.map(x=><a className="block border-b border-white/15 py-4 text-sm" key={x.href} onClick={()=>setOpen(false)} href={x.href}>{x.label}</a>)}</nav>}</header>}
