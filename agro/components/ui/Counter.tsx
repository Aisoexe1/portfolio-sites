"use client";import {useEffect,useRef,useState} from "react";export function Counter({value}:{value:string}){const[valueNow,setValueNow]=useState("0");const ref=useRef<HTMLSpanElement>(null);useEffect(()=>{const el=ref.current;if(!el)return;const observer=new IntersectionObserver(([entry])=>{if(!entry.isIntersecting)return;let start=0;const end=Number(value.replace(" ","").replace(",","."));const t0=performance.now();const tick=(t:number)=>{const p=Math.min((t-t0)/900,1);start=Math.round(end*p*10)/10;setValueNow(new Intl.NumberFormat("uk-UA",{maximumFractionDigits:1}).format(start));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);observer.disconnect()},{threshold:.5});observer.observe(el);return()=>observer.disconnect()},[value]);return <span ref={ref}>{valueNow}</span>}
